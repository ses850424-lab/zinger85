# 미션 외관세척기 — 제어 프로그램(HMI + 통신 미들웨어) 개발 프로젝트 플랜

- 작성일: 2026-08-31
- 근거 문서: `세척기_제어시스템_기획_20260826.md` (작성일 2026-08-26, 프로젝트 문서함에 보관) — 이하 "기획서"로 표기
- 이 플랜의 범위: **PLC는 외주 제작**이므로 제외. 여기서 만드는 것은 **HMI 화면(산업용PC, HTML/JS)** 과 **PLC와 HMI를 잇는 통신 미들웨어(Node.js, Modbus TCP)** 두 가지 소프트웨어다.

---

## 1. 왜 이 구조인가 — 기획서 근거 요약

기획서 1항(설비 개요)에 따르면 역할 분담은 다음과 같이 이미 확정되어 있다.

| 영역 | 담당 | 비고 |
|---|---|---|
| 기계설비 | 사용자 직접 제작 | 본 플랜 범위 아님 |
| PLC (시퀀스 로직 + 안전인터록 + 서보/인버터 지령) | 외주 제작 | 본 플랜 범위 아님 — 단, I/O 태그맵은 PLC 발주 사양서의 기초가 되므로 여기서 함께 정리 |
| HMI 화면 + 통신 미들웨어 | **Claude와 공동 제작 (본 플랜의 대상)** | 산업용PC + HTML/JS + Node.js + Modbus TCP |

기획서 5항에서 "안전판단은 PLC/하드와이어드에서 하고 HMI 화면은 상태 표시만 한다"는 원칙이 2026-08-26에 확정되어 있다. 이 원칙은 설계 전체를 관통하는 제약조건이므로, 폴더 구조와 로드맵 모두 이 전제 위에서 세운다 — **즉, 이 프로그램(HMI/미들웨어)은 안전 인터록을 대체하거나 우회하는 로직을 갖지 않는다. 표시와 지령 전달만 담당한다.**

---

## 2. 개발 대상 신호 분류 (기획서 2·4항 근거)

폴더 구조를 모터/실린더/센서 세 갈래로 나눈 근거는 기획서 2항(액추에이터 구성)과 4항(I/O 리스트)의 분류를 그대로 따른 것이다.

### 2-1. 모터 제어 (server/src/io/motor/)
| 신호 | 구동방식 | 통신/지령 방식 | 상태(기획서 기준) |
|---|---|---|---|
| 노즐 X축 | 서보모터 | 서보앰프 통신/펄스 (방식 미확정) | 사양 미확정 — 리드·스트로크·최대속도 확인 필요 |
| 노즐 Z축 | 서보모터 1대 + 볼스크류 2개 + 감속기(10:1) + 라인샤프트 | 서보앰프 통신/펄스 (방식 미확정) | [[볼스크류2축_서보모터선정_계산서.xlsx]] 스펙 적용: 이송질량 70kg, 리드 5mm, 최대속도 25mm/s, 축간거리 1000mm, 기준토크(SF1.5) 약 0.1197N·m(Case1)~0.1095N·m(Case3), 모터 3000rpm, 50~100W급 검토 |
| 파렛트 회전 | AC모터 + 인버터(VFD) | 아날로그(0-10V/4-20mA) 또는 통신(Modbus RTU 등) — 방식 미확정 | HMI에서 속도 실시간 조절 가능해야 함 (약 5초/회전 = 12rpm 부근에서 튜닝 시작 예상, 확정값 아님) |

### 2-2. 실린더 제어 (server/src/io/cylinder/)
| 신호 | 구동방식 | 출력 | 관련 입력(완료확인) |
|---|---|---|---|
| 파렛트 위치이동 | 에어실린더 (전진/후진) | 솔레노이드밸브 | 전진완료 근접센서 / 후진완료 근접센서(존재여부 확인 필요) |
| 파렛트 리프트 업/다운 | 에어실린더 (모터 아님 — 기획서에서 이전 검토 정정됨) | 솔레노이드밸브 | 상승완료 센서 / 하강완료 센서 (종류 확인 필요) |

### 2-3. 센서 온오프 (server/src/io/sensor/)
| 신호 | 종류 | 비고 |
|---|---|---|
| 안착확인 버튼 | 푸시버튼 | 작업자 조작 |
| 안전도어 닫힘·잠김 | 인터록 스위치 | 이중화 권장(기계식 리밋스위치 + 비접촉식 인터록) — 설계 중 |
| 비상정지 버튼 | E-STOP (하드와이어드) | PLC/하드와이어드가 직접 차단, HMI는 상태표시만 |
| 고압펌프 이상신호 | (있다면) | 확인 필요 |
| 노즐 X·Z축 원점/리미트 | 서보 관련 | 구성 확인 필요 |

이 세 폴더(motor/cylinder/sensor)는 각각 **"Modbus 레지스터를 읽고/쓰는 얇은 어댑터 계층"**이다 — 실제 판단(자동시퀀스 진행 여부, 안전정지 여부)은 PLC가 하고, 이 코드는 PLC가 넘겨준 레지스터 값을 HMI가 쓸 수 있는 형태로 변환하거나, HMI에서 입력된 값(예: 회전속도 설정값)을 PLC가 받는 레지스터 형식으로 변환하는 역할만 한다.

---

## 3. 폴더 구조

```
industrial-equipment-control/
├── PROJECT_PLAN.md                 ← 본 문서
├── docs/
│   ├── 01_설비개요.md               ← 기획서 1~2항 요약(설비 개요, 액추에이터 구성)
│   ├── 02_IO_태그맵.md              ← 기획서 4항 I/O 리스트 → Modbus 레지스터 매핑표 (미확정, 확정 시 갱신)
│   ├── 03_공정시퀀스.md             ← 기획서 3항 0~9단계 시퀀스 → 상태머신 설계 근거
│   └── 04_확인필요사항.md           ← 기획서 7항 미확정 항목 추적 (체크리스트, 진행상황 갱신용)
├── server/                         ← Node.js 통신 미들웨어 (HMI ↔ PLC)
│   ├── package.json
│   ├── src/
│   │   ├── modbus/
│   │   │   ├── client.js           ← PLC와의 Modbus TCP 클라이언트 연결/재접속 로직
│   │   │   └── registerMap.js      ← docs/02_IO_태그맵.md 를 코드 상수로 옮긴 것 (단일 진실 소스)
│   │   ├── io/
│   │   │   ├── motor/              ← §2-1 모터 제어 어댑터 (서보 X/Z축, 인버터 회전속도)
│   │   │   ├── cylinder/           ← §2-2 실린더 제어 어댑터 (위치이동, 리프트)
│   │   │   └── sensor/             ← §2-3 센서 상태 읽기 어댑터 (근접센서, 도어, E-STOP 상태표시)
│   │   ├── sequence/
│   │   │   └── stateMachine.js     ← 0~9단계 상태를 HMI에 표시하기 위한 상태 추적 (판단은 PLC, 여기는 표시용 미러링만)
│   │   ├── api/
│   │   │   ├── wsServer.js         ← HMI와의 실시간 통신(WebSocket) — 상태 push, 명령 수신
│   │   │   └── restRoutes.js       ← 설정값 조회/변경 등 REST 엔드포인트
│   │   └── config/
│   │       └── plcConfig.js        ← PLC IP/포트/폴링주기 등 환경설정
│   └── test/                       ← 유닛테스트 (시뮬레이터 대상으로 실행)
├── simulator/                      ← PLC 없이 선행 개발하기 위한 Modbus TCP 가상 PLC
│   └── src/
│       └── mockPlcServer.js        ← registerMap.js 기준으로 가상 레지스터를 만들어 실제 PLC처럼 응답
├── hmi/                            ← 산업용PC 화면 (HTML/JS)
│   ├── index.html
│   ├── css/
│   ├── js/
│   │   ├── screens/
│   │   │   ├── mainScreen.js       ← 자동모드: 사이클 시작/정지, 현재 단계 표시, 회전속도 슬라이더
│   │   │   ├── manualScreen.js     ← 수동모드: 실린더 개별 전후진, 노즐 X/Z 개별 이동 (셋업/유지보수용)
│   │   │   └── alarmScreen.js      ← 알람이력, 사이클 카운트, 인터록/비상정지 상태표시
│   │   ├── components/             ← 재사용 UI 부품 (속도 슬라이더, 상태 인디케이터 등)
│   │   └── wsClient.js             ← server/src/api/wsServer.js 와의 통신 클라이언트
│   └── assets/
└── logs/                           ← 알람이력·사이클카운트 등 런타임 로그 저장 위치
```

**폴더 구조 설계 원칙**: `io/motor`, `io/cylinder`, `io/sensor` 로 나눈 것은 기획서의 액추에이터 분류(2항)·I/O 분류(4항)와 1:1로 대응시켜, 나중에 I/O 태그맵이 바뀌어도 "무엇을 고쳐야 하는지"가 폴더명만 보고 바로 판단되게 하기 위함이다. `simulator/`를 `server/`와 분리한 이유는 기획서 8항 "Modbus TCP 시뮬레이터로 PLC 없이 선행 개발"을 그대로 반영한 것 — 실제 PLC 연동 전까지는 `simulator/`만 켜놓고 `server/`와 `hmi/`를 완성할 수 있다.

---

## 4. 단계별 개발 로드맵

각 단계는 기획서 7항(확인 필요 사항)과 8항(다음 단계)을 그대로 이어받아 순서를 정한 것이다. 괄호 안은 완료 조건.

### Phase 0 — 준비 (진행 중, 선행조건 확인 대기)
- 목표: I/O 태그맵을 확정할 수 있는 최소 정보 확보
- 산출물: `docs/04_확인필요사항.md` 체크리스트 답변 수렴
- 완료 조건: 아래 8개 항목 중 최소한 "서보앰프-PLC 통신방식"과 "인버터-PLC 통신방식"이 확정되어야 Phase 2(레지스터 매핑)를 시작할 수 있음
  - 노즐 X축 서보 사양(리드·스트로크·최대속도)
  - 위치이동실린더 후진완료 센서 유무
  - 리프트실린더 상승/하강완료 센서 종류
  - 노즐 X·Z축 원점·리미트 센서 구성
  - 서보앰프-PLC 통신방식 (펄스열 / EtherCAT 등)
  - 인버터-PLC 통신방식 (아날로그 vs 통신)
  - 안전도어 설계 확정본
  - 세척 사이클 목표시간(초기값)

### Phase 1 — 시뮬레이터 및 미들웨어 기초 (Phase 0과 병행 가능)
- 목표: 실제 PLC 없이 개발이 가능한 최소 환경 구축
- 산출물: `simulator/src/mockPlcServer.js` (가상 레지스터, on/off 시뮬레이션), `server/src/modbus/client.js` (연결/재접속/타임아웃 처리)
- 완료 조건: `server`가 `simulator`에 접속해 임의 레지스터를 읽고 쓸 수 있음 (실제 신호 의미는 아직 몰라도 됨)

### Phase 2 — I/O 매핑 & 어댑터 구현
- 목표: 확정된 태그맵을 `registerMap.js`에 반영하고 `io/motor`, `io/cylinder`, `io/sensor` 어댑터 작성
- 산출물: `docs/02_IO_태그맵.md` 확정판, 대응 어댑터 코드
- 완료 조건: 시뮬레이터 기준으로 모터 속도지령 전달, 실린더 전후진 명령, 센서 상태 읽기가 각각 단위테스트 통과

### Phase 3 — 시퀀스 상태머신 (표시 전용)
- 목표: 기획서 3항 0~9단계를 HMI에 보여주기 위한 상태 추적 로직
- 산출물: `server/src/sequence/stateMachine.js`
- 설계 제약: **이 상태머신은 진행을 "결정"하지 않는다.** PLC가 이미 진행시킨 결과(레지스터 값)를 읽어 "지금 몇 단계인지"를 미러링만 한다 — 안전판단 이중화 금지 원칙(기획서 5항) 준수
- 완료 조건: 시뮬레이터에서 레지스터 값을 단계별로 바꿔가며 상태머신이 0→9까지 정확히 따라감을 확인

### Phase 4 — HMI 화면 개발
- 목표: 기획서 6항 요구사항 구현
- 산출물: `hmi/js/screens/mainScreen.js`(자동모드), `manualScreen.js`(수동모드), `alarmScreen.js`(알람/이력)
- 포함 기능: 자동/수동 모드 전환, 사이클 시작/정지 및 단계표시, 파렛트 회전속도 설정(슬라이더), 도어·인터록·비상정지 상태표시(표시만), 알람이력·사이클카운트, 수동모드 개별 조작
- 완료 조건: 시뮬레이터 연동 상태에서 화면 조작 → WebSocket 명령 전송 → 시뮬레이터 레지스터 반영까지 왕복 확인

### Phase 5 — 통합 테스트 (시뮬레이터 기준)
- 목표: 0~9단계 전체 사이클을 시뮬레이터로 모의 실행
- 산출물: `server/test/` 시나리오 테스트 (정상 사이클, 도어 열림 시 즉시정지, 비상정지 시 즉시정지 등)
- 완료 조건: 기획서 3항 "공통(전 구간)" 규칙 — 비상정지·도어열림 시 즉시정지 — 가 시뮬레이터 상에서 모두 재현됨

### Phase 6 — 실제 PLC 연동 및 현장 튜닝
- 목표: PLC 납품 후 `simulator` 대신 실제 PLC에 연결, 현장에서 파라미터 튜닝
- 산출물: 최종 배포판, 튜닝값 반영 (회전속도 초기값 12rpm 부근 등 현장 확인 후 확정)
- 완료 조건: 외주 PLC업체와 통합테스트 통과, 안전인터록 최종 검증(기계안전 인증 컨설턴트 확인 권장 — 기획서 5항)

---

## 5. 리스크 및 설계 원칙 재확인 (기획서 5항 근거)

1000bar는 신체 절단·관통 손상이 가능한 등급이므로, 이 프로그램은 다음 원칙을 반드시 지킨다.
- 안전 판단(도어 잠금 확인, 비상정지 등)은 **PLC/하드와이어드**의 몫이며, 이 소프트웨어(HMI/미들웨어)는 **상태를 표시만** 한다.
- 고압펌프 기동 회로·비상정지 회로는 하드와이어드로 별도 구성되어 있다고 전제하며, 이 프로그램의 소프트웨어 로직으로 그 역할을 대신하지 않는다.
- 정확한 법적 기준(산업안전보건법령, KOSHA 기술지침 등)은 외주 PLC업체 또는 기계안전 인증 컨설턴트가 별도 확인해야 하며, 이 프로젝트 플랜은 그 역할을 대신하지 않는다 (참고: KOSHA 자료마당 https://www.kosha.or.kr/kosha/data/guidanceG.do).

---

## 6. 다음 액션 아이템

1. `docs/04_확인필요사항.md`의 8개 항목에 대한 답변 수렴 (Phase 0)
2. 답변이 나오는 대로 `docs/02_IO_태그맵.md`에 Modbus 레지스터 주소 확정
3. Phase 1(시뮬레이터) 코드는 태그맵 확정을 기다리지 않고 지금 바로 착수 가능 — 병행 진행 권장
