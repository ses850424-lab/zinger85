# CLAUDE.md

이 프로젝트에서 Claude Code로 작업할 때 지키는 최소 지침. 진행하면서 필요한 항목을 아래에 계속 추가한다.

## 프로젝트
미션 외관세척기(1000bar 초고압수 세척설비)의 **HMI 화면 + PLC 통신 미들웨어**. PLC 자체(시퀀스·안전인터록)는 외주 제작이라 이 저장소 범위가 아님.

## 반드시 지킬 것
- **안전판단은 이 코드가 하지 않는다.** 도어 인터록, 비상정지 등은 PLC/하드와이어드가 전담 — 이 프로그램은 상태 표시와 지령 전달만 한다. (근거: PROJECT_PLAN.md §5) 단, `moveTo`/`setSpeed` 등에서 음수·범위초과 값을 막는 건 안전판단이 아니라 단순 입력값 방어이므로 이런 검증은 계속 넣는다 — "안전판단 금지"를 이유로 걷어내지 않는다.
- **모르는 값은 임의로 채우지 않는다.** 통신방식·센서 종류 등 미확정 항목은 `docs/04_확인필요사항.md`에 남겨두고, 확정되면 그때 채운다. 코드에서 미확정 설정값을 다뤄야 할 때는 `null`로 두고 사용하는 쪽에서 `설정값 || DEFAULT_X` 형태로 기본값 상수를 옆에 정의하는 방식을 따른다 (`plcConfig.js` 참고). 코드 주석에 "확인 필요/제안값"을 남겼으면 `docs/04_확인필요사항.md`에도 같은 항목을 반드시 추가한다 — 코드에만 남기고 문서에 빠뜨리지 않는다.
- **문서 → 코드 순서.** I/O 태그맵이 바뀌면 `docs/02_IO_태그맵.md`를 먼저 고치고, `server/src/modbus/registerMap.js`에 그대로 반영한다. 코드를 먼저 고치고 문서를 나중에 맞추지 않는다.
- **코드는 한글 주석을 단다.** 특히 테스트/검증 단계 코드(자가테스트, 시뮬레이터, 워치독 등)는 무엇을/왜 테스트하는지 한글로 설명한다. 함수·로직 블록 단위로 목적이 드러나게 주석을 붙인다.
- **HMI ↔ 서버 통신 포맷을 임의로 바꾸지 않는다.** HMI→서버는 `{type:"command", action, payload}`(`action`은 `wsServer.js`의 `COMMAND_HANDLERS` 키와 1:1 대응), 서버→HMI는 `{type:"state", ...snapshot}` 폴링 브로드캐스트. HMI 화면 코드(`screens/*.js`)는 WebSocket을 직접 만지지 않고 `window.wsClient.send(action, payload)` / `wsClient.onState(cb)`만 사용한다.
- **HMI는 빌드 도구 없는 순수 스크립트 태그 구조다.** `window.wsClient`, `window.showScreen` 같은 전역으로 화면끼리 통신하고 `index.html`에서 `<script>` 태그를 순서대로 로드한다(`wsClient.js`가 항상 먼저). 번들러나 `import`/`export` 모듈 문법을 도입하지 않는다.
- **`server/`와 `simulator/`의 설정값은 자동 동기화되지 않는다.** `registerMap.js`는 `simulator/`가 상대경로로 직접 require해서 공유되지만, host/port 등은 `server/src/config/plcConfig.js`와 `simulator/src/mockPlcServer.js`에 각각 따로 있다. 이런 설정을 고칠 땐 두 파일을 함께 확인한다.
- **테스트는 `node server/test/*.manual.js`로 직접 실행한다.** 시뮬레이터(`simulator/`)를 먼저 켜둔 상태에서 실행. `npm test`는 아직 placeholder라 실패하는 게 정상이다.
- **PLC 태그에 대응하는 도메인 식별자(레지스터 키 등)는 한글, 함수·변수·파일명은 영어 camelCase.** `registerMap.js`의 `안착확인버튼` 같은 키가 예시. 이 둘을 섞어 쓰지 않는다.
- **알람/카운터는 off→on 엣지에서만 기록한다.** 알람은 `logs/alarms.log`(JSONL)에 추가 + 메모리에 최근 20개만 유지, 사이클카운트는 9단계 진입 시점에만 증가시켜 `logs/cycleCount.json`에 저장. 새 알람·카운터를 추가할 때도 이 패턴(엣지 트리거, 파일 위치)을 따른다.

## 참고 문서 (읽는 순서)
1. `PROJECT_PLAN.md` — 전체 로드맵, 현재 어느 Phase인지
2. `docs/04_확인필요사항.md` — 지금 막혀있는 게 뭔지
3. `docs/02_IO_태그맵.md`, `docs/03_공정시퀀스.md` — 실제 구현 시 참조

## 폴더 규칙
- `server/src/io/motor|cylinder|sensor` — 신호 종류별로만 나눔, 새 신호는 해당 폴더에 추가
- `simulator/` — 실제 PLC 없을 때 여기부터 개발/테스트
- `hmi/` — 화면 담당, PLC 통신 로직을 직접 넣지 않고 `server`를 거친다

---
<!-- 아래부터 프로젝트 진행하며 추가 -->
전기제어 전문가 관점 검토 (사용자가 "전문가 관점으로 검토해줘" 요청 시 이 체크리스트로 확인)
Fail-safe 여부: 신호선 단선·통신 끊김·정전 시 위험한 쪽(기동/전진)이 아니라 안전한 쪽(정지)으로 가는 구조인지. 
특히 비상정지·도어인터록 계열 입력은 관례적으로 NC(normally closed) 접점을 쓴다 — 단선되면 "정지 신호 들어옴"으로 해석되게. 
이 코드가 NC/NO 어느 쪽을 가정하는지 주석에 명시되어 있는지 확인.
인터록 우회 가능성: 어떤 코드 경로에서도 도어인터록·비상정지 상태를 무시하거나, 타임아웃으로 그냥 넘어가거나, 재시도 로직이 안전신호를 마스킹하지 않는지.
통신 단절 시 동작: Modbus 연결이 끊기면 마지막 값을 그대로 유지(hold)하지 않고 안전한 정지 상태로 폴백하는지 (client.js 재접속 로직에 이 처리 포함되어야 함).
디바운싱: 리밋스위치·버튼 등 기계식 접점은 순간적으로 on/off를 반복(채터링)할 수 있음 — 이 값을 그대로 사이클 진행 판단에 쓰면 오동작 가능. 디바운스 처리가 PLC/코드 어느 쪽 책임인지 명시.
복전(정전 후 재기동) 시 동작: 전원이 다시 들어왔을 때 이전 사이클을 자동으로 이어가지 않고 초기 상태(대기)로 돌아가는지.
네이밍/주소 일치: registerMap.js의 이름·주소가 docs/02_IO_태그맵.md와 정확히 일치하는지, PLC 발주 사양서와 어긋난 곳은 없는지.
관련 규격 언급 시 주의: IEC 61508(기능안전), ISO 13849-1(기계류 안전 관련 제어시스템), NFPA 79, KOSHA 지침 등을 언급할 순 있으나, 이 프로젝트가 실제로 특정 규격을 준수한다고 Claude가 임의로 단정하지 않는다 — 규격 적합성 판단은 기계안전 인증 컨설턴트의 몫(PROJECT_PLAN.md §5 원칙과 동일).